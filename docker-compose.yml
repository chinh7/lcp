version: '3'
services:
  node:
    volumes:
      - ./data:/root/.tendermint
    ports:
      - 26657:26657
      - 5555:5555
    build:
      context: .
      dockerfile: ./dockers/vertex.Dockerfile
    command: node
